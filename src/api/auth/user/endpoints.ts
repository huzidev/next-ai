const AUTH_USER = "/api/auth/user";

export const SIGNUP = `${AUTH_USER}/signup`;
export const SIGNIN = `${AUTH_USER}/signin`;
export const VERIFY = `${AUTH_USER}/verify`;
export const FORGOT_PASSWORD = `${AUTH_USER}/forgot-password`;
